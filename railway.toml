[build]
builder = "RAILPACK"
buildCommand = "yarn build"

[deploy]
startCommand = "yarn db:generate && yarn db:migrate && yarn start -H 0.0.0.0 -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
